# Frontend

This is the React + Vite frontend for the School ERP system.

## Environment

Create `frontend/.env` from `frontend/.env.example` and set:

```bash
VITE_SUPABASE_URL=...
VITE_SUPABASE_ANON_KEY=...
```

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
